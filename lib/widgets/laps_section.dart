import 'package:flutter/material.dart';

class LapsSection extends StatelessWidget {
  const LapsSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
